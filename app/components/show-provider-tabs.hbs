    <nav class="tab-content nav-tabs-member" aria-label="Provider sections">
      <BsNav
        @type="tabs"
        @justified={{false}}
        @stacked={{false}}
        @fill={{false}} as |nav|
      >
        <nav.item>
            <nav.linkTo @route='providers.show.index' @current-when='providers.show.index' class='nav-link'>
                Info
            </nav.linkTo>
        </nav.item>
        <nav.item>
            <nav.linkTo @route='providers.show.settings' @current-when='providers.show.settings' class='nav-link'>
                Settings
            </nav.linkTo>
        </nav.item>
        {{#if (eq this.model.memberType 'consortium')}}
          <nav.item>
              <nav.linkTo @route='providers.show.organizations' @current-when='providers.show.organizations' class='nav-link'>
                  Consortium Organizations
              </nav.linkTo>
          </nav.item>
        {{/if}}
        {{#if (not-eq this.model.memberType 'developer')}}
          <nav.item>
              <nav.linkTo @route='providers.show.contacts' @current-when='providers.show.contacts' class='nav-link'>
                  Contacts
              </nav.linkTo>
          </nav.item>
          {{#if (not-eq this.model.memberType 'member_only')}}
            <nav.item>
                <nav.linkTo @route='providers.show.repositories' @current-when='providers.show.repositories' class='nav-link'>
                    Repositories
                </nav.linkTo>
            </nav.item>
            <nav.item>
                <nav.linkTo @route='providers.show.prefixes' @current-when='providers.show.prefixes' class='nav-link'>
                    Prefixes
                </nav.linkTo>
            </nav.item>
            <nav.item>
                <nav.linkTo @route='providers.show.dois' @current-when='providers.show.dois' class='nav-link'>
                    DOIs
                </nav.linkTo>
            </nav.item>
          {{/if}}
        {{/if}}
      </BsNav>
    </nav>
  