{{#if (can "read index")}}
  <ApplicationHeader @default={{true}} />

  <div class="container-fluid">
    <IndexHeader />
    <RepositoryList @model={{model}} @link="repositories" @searchable={{true}} />
  </div>
{{else}}
  <ApplicationHeader />
  <LandingPage />
{{/if}}
