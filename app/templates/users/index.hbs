{{#if (can "read index")}}
  <ApplicationHeader @default={{true}} />

  <div class="container-fluid">
    <IndexHeader />
    <UserList @model={{this.model}} @searchable={{true}} />
  </div>
{{else}}
  <ApplicationHeader />
  <LandingPage />
{{/if}}
