{{#if (can "read index")}}
  <ApplicationHeader @default={{true}} />

  <div class="container-fluid">
    <IndexHeader />
    <ProviderList @model={{model}} @searchable={{true}} />
  </div>
{{else}}
  <ApplicationHeader />
  <LandingPage />
{{/if}}
