{{#if (can "read index")}}
  {{application-header default=true}}

  <div class="container-fluid">
    {{index-header}}
    {{provider-list model=model searchable=true}}
  </div>
{{else}}
  {{application-header}}
  {{landing-page}}
{{/if}}
