    <div class="container-fluid"     style="padding-left: 0px;">
      <div class="row" style="display:flex">
            {{#if citations}}
            <p style="margin-right: 1.5em;">{{fa-icon "quote-right"}}  {{ pluralize citations 'Citation' }}</p>
            {{/if}}
            {{#if views}}
               <p style="margin-right: 1.5em;"> {{fa-icon "eye"}}  {{ pluralize views 'View' }} </p>
            {{/if}}
            {{#if downloads}}
             <p style="margin-right: 1.5em;"> {{fa-icon "download"}}   {{ pluralize downloads 'Download' }} </p>
            {{/if}}
      </div>
    </div>
