<div class="row">
  <div class="col-md-9">
    <h3 class="member-results">URL</h3>
    <div class="panel panel-transparent url">
      <div class="panel-body">
        {{#if model.url}}
          {{url-check model=model}}
        {{else}}
          The URL is currently not available.
        {{/if}}
      </div>
    </div>
  </div>
</div>
