<ApplicationHeader @sign-in={{true}} />

<div class="wrapper">
  <div class="register-background" style="background-image: url('https://datacite.org/images/fabrica/header.png');">
    <div class="container">
      <div class="row">
        <div class="col-md-4 col-md-offset-4 col-sm-6 col-sm-offset-3 col-xs-10 col-xs-offset-1 ">
          <div class="register-card">
            <h3 class="title">Sign In</h3>
            
            {{#if showGlobus}}
              <a href="{{oidcAuthUrl}}" class="btn btn-block btn-social btn-globus btn-fill"><img id="globus-logo" src="{{globusLogoUrl}}" alt="Globus icon"/> Sign In with Globus</a>
              
              <div id="or">or</div>
            {{/if}}

            <BsForm @formLayout="vertical" @model={{this}} @onSubmit={{action "authenticate"}} @submitOnEnter={{true}} as |form|>
              <form.element @controlType="text" @property="identification" @id="account" @label="Account ID" @required={{false}} data-test-account/>
              <form.element @controlType="password" @property="password" @id="password" @label="Password" @required={{false}} data-test-password />

              {{#if errorMessage}}
                <div class="alert alert-danger">{{errorMessage}}</div>
              {{/if}}

              <div>
                <LinkTo @route="reset">Forgot password?</LinkTo> •
                <a target="_blank" rel="noopener" href="https://support.datacite.org/docs/create-a-fabrica-account">Need an account?</a>
              </div>

              <div class="pseudo-footer">
                <div class="btn-toolbar pull-right">
                  <button type="submit" class="btn btn-sm btn-fill">Sign In</button>
                </div>
                <div class="clearfix"/>
              </div>
            </BsForm>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
