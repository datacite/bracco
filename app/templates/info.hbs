{{#if (can "read index")}}
  <ApplicationHeader @default={{true}} />

  <div class="container-fluid">
    <IndexHeader />

    <IndexInfo @model={{this.model}} />
  </div>  
{{else}}
  <ApplicationHeader />
  <LandingPage />
{{/if}}
