{{#if (can "read index")}}
  <ApplicationHeader @default={{true}} />

  <div class="container-fluid">
    <IndexHeader />
    {{yield}}
  </div>
{{else}}
  <ApplicationHeader />
  <LandingPage />
{{/if}}
