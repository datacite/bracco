{{#if (can "read index")}}
  {{application-header default=true}}

  <div class="container-fluid">
    {{index-header}}
    {{provider-show model=model admin=true}}
  </div>
{{else}}
  {{application-header}}
  {{landing-page}}
{{/if}}
