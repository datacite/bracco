{{#if (can "read client" model)}}
  {{application-header default=true}}

  <div class="container-fluid">
    {{#if (can "read provider" model.provider)}}
      <ol class="breadcrumb">
        {{#if (can "read index")}}
          <li class="breadcrumb-item">{{#link-to "index"}}DataCite{{/link-to}}</li>
          <li class="breadcrumb-item">{{#link-to "providers"}}Providers{{/link-to}}</li>
        {{/if}}
        {{#if (eq model.provider.memberType "consortium_organization")}}
          <li class="breadcrumb-item">{{#link-to "providers.show.index" model.provider.consortiumLead.id}}{{model.provider.consortiumLead.name}}{{/link-to}}</li>
        {{/if}}
        <li class="breadcrumb-item">{{#link-to "providers.show" model.provider.id}}{{model.provider.name}}{{/link-to}}</li>
        {{#if (can "read index")}}
          <li class="breadcrumb-item">{{#link-to "providers.show.periodicals" model.provider.id}}Periodicals{{/link-to}}</li>
        {{else}}
          <li class="breadcrumb-item">{{#link-to "providers.show.clients" model.provider.id}}Clients{{/link-to}}</li>
        {{/if}}
      </ol>
    {{/if}}

    <div id="show">
      <h2 class="work">{{model.name}}{{#if (not model.isActive)}} <span class="small icon-warning">account not active</span>{{/if}}</h2>

      <div class="tab-content nav-tabs-member">
        {{#bs-nav type="tabs" as |nav|}}
          {{#nav.item}}{{#nav.link-to "periodicals.show.index" model.id class="nav-link"}}Info{{/nav.link-to}}{{/nav.item}}
          {{#nav.item}}{{#nav.link-to "periodicals.show.settings" model.id class="nav-link"}}Settings{{/nav.link-to}}{{/nav.item}}
          {{#nav.item}}{{#nav.link-to "periodicals.show.prefixes" model.id class="nav-link"}}Prefixes{{/nav.link-to}}{{/nav.item}}
          {{#nav.item}}{{#nav.link-to "periodicals.show.dois" model.id class="nav-link doi"}}DOIs{{/nav.link-to}}{{/nav.item}}
        {{/bs-nav}}
      </div>
    </div>

    {{outlet}}
  </div>
{{else}}
  {{application-header}}
  {{landing-page}}
{{/if}}
