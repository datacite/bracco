{{#if (can "read index")}}
  {{application-header default=true}}

  <div class="container-fluid">
    {{index-header}}
    {{periodical-list model=model link="periodicals" searchable=true}}
  </div>
{{else}}
  {{application-header}}
  {{landing-page}}
{{/if}}
