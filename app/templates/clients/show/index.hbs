<div class="row">  
  {{chart-doi model=model}}
</div>

{{#if (can "read messages" model)}}
  <div class="row">
    <div class="col-md-9">
      {{#each flashMessages.queue as |flash|}}
        {{flash-message flash=flash}}
      {{/each}}
    </div>
  </div>
{{/if}}
