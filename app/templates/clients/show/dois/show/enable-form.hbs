<div class="col-md-9 col-md-offset-3">
  <h2 class="doi">{{model.doi}}</h2>
</div>

<div class="panel panel-transparent">
  <div class="panel-body">
    <div class="col-md-9 col-md-offset-3">
      {{#bs-form formLayout="horizontal" horizontalLabelGridClass="col-md-3" model=model onSubmit=(action "enable"
      model) as |form|}}

      <p>
        This will switch the source of DOI registration to the form, which in turn enables the form to be used for
        editing DOI metadata.
        Please ensure that the metadata does not contain any data that could be overwritten by the formn.
      </p>

      <div class="pseudo-footer">
        <div class="btn-toolbar pull-right">
          <button {{action "cancel"}} class="btn btn-sm">Cancel</button>
          <button type="submit" class="btn btn-sm btn-fill">Enable</button>
        </div>
        <div class="clearfix" />
      </div>
      {{/bs-form}}
    </div>
  </div>
</div>