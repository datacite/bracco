import bugsnag from 'bracco/utils/bugsnag';
import { module, test } from 'qunit';

module('Unit | Utility | bugsnag', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = bugsnag();
    assert.ok(result);
  });
});
